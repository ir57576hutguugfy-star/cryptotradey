---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import { CATEGORIES } from '../../consts';
import { breadcrumbSchema } from '../../lib/seo';
import { toBase } from '../../lib/url';

const crumbs = [
  { name: 'Início', path: '/' },
  { name: 'Aprenda', path: '/aprenda/' },
];
---

<BaseLayout
  title="Aprenda sobre criptomoedas"
  description="Guias e tutoriais de criptomoedas para brasileiros, organizados por tema: iniciantes, como fazer, carteiras e segurança."
  canonical="/aprenda/"
  schema={[breadcrumbSchema(crumbs)]}
>
  <div class="container">
    <Breadcrumbs items={crumbs} />
    <h1>Aprenda</h1>
    <p class="lead">Guias e tutoriais organizados por tema.</p>

    <div class="card-grid">
      {CATEGORIES.map((cat) => (
        <a class="card" href={toBase(`/aprenda/${cat.slug}/`)}>
          <h3>{cat.name}</h3>
          <p>{cat.description}</p>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>
